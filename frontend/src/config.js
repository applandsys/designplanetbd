const config = {
    apiBaseUrl: process.env.NEXT_PUBLIC_API_BASE_URL || "https://backend.designplanetbd.com/v1",
    publicPath: process.env.NEXT_PUBLIC_PATH || "https://backend.designplanetbd.com"
};

export default config;